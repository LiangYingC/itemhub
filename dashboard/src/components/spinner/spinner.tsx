import spinnerIcon from '@/assets/images/spinner.svg';
const Spinner = () => {
    return (
        <div className="spinner">
            <img src={spinnerIcon} />
        </div>
    );
};

export default Spinner;
